# PrismStar — 意思決定ログ（Decision Log / ADR-lite）

このファイルは PrismStar（通称 Prism）開発の **重要な意思決定と理由** を時系列で固定する。
「何を作るか」だけでなく「**なぜそう決めたか / 何を却下したか**」を残すことが、
本プロジェクトを **AI 駆動開発のポートフォリオ**として成立させる中核である。

書式：各決定は `背景 / 決定 / 理由 / 代替案・却下理由 / 状態`。
状態凡例：✅ 採用中 / 🔄 見直し対象 / ❌ 撤回

---

## ADR-001 作業ベースは `develop` ブランチ ✅
- **背景**：当初 `main` を見ていたが、`develop` に PHP + MySQL + Docker のバックエンドが既に実装済みだった。
- **決定**：今後の作業は `develop` を基点にする。
- **理由**：develop が本体で大幅に先行（マルチユーザー認証・ギャラリー CRUD・お問い合わせが実際に動作）。`main` は古い。
- **代替案**：`main` で進める → 既存の動く実装を捨てることになり却下。

## ADR-002 バックエンドは PHP + MySQL + Docker を維持（Node に書き換えない）✅
- **背景**：当初 Node + Express を検討した。しかし develop は既に PHP で実装済み。
- **決定**：PHP 8.2 + MySQL 8.0 + Docker Compose を維持する。
- **理由**：動いている実装を作り直すのは「**必要だから使う、無理には使わない**」という本人の原則に反する。Docker が「主要技術を触りたい」要件も満たしている。
- **代替案**：Node で再実装 → 価値のない作り直しのため却下。

## ADR-003 GitHub API はバックエンド経由（フロント直叩きにしない）✅
- **背景**：GitHub 連携で API をどこから呼ぶか（フロント直叩き vs サーバ経由）。
- **決定**：サーバ側 PHP（`api/github.php`）から呼び、token は環境変数に隠す。
- **理由**：フロントに token を置くとソースに丸見えで漏洩する。認証付き（5000 req/h）の安定アクセスを「**安全に**」得るには token をサーバ側に隠すしかない。「両者を比較して安全な方を選んだ」という意思決定自体がポートフォリオ価値になる。
- **代替案**：フロント直叩き（未認証 60 req/h、または token 漏洩）→ 却下。
- **関連**：`docs/ai-roles-and-workflow.md` §4 safety invariants。

## ADR-004 コンセプトは方向A（自前ギャラリー＋GitHub取り込み）✅
- **背景**：純粋な GitHub リポジトリ・ショーケースにするか、自前の投稿サイトを残すか。
- **決定**：自前マルチユーザー・ギャラリー（サイトのユーザーが画像 / 文字を投稿）を残し、GitHub 連携を**追加機能**として乗せる。
- **理由**：develop の資産を活かせる。「サイト独自ユーザー（非エンジニアでも投稿可）」という要望も満たせる。
- **代替案**：GitHub リポジトリだけのショーケース（方向B）→ develop の投稿機能を捨てるため却下。
- **発展**：[ADR-008](#adr-008-全員が発信者プラットフォーム化-)で「全員が発信者」プラットフォームへ拡張。

## ADR-005 AI 運用は Claude Code + Codex の3者構成（ChatGPT なし）✅
- **背景**：参照元 `origin-egents` は User / ChatGPT / Claude Code / Codex の4者・一方向フロー。
- **決定**：ChatGPT を抜いた **User / Claude Code / Codex の3者**。実装は原則 Codex へ handoff、Claude Code は設計と handoff docs 作成、直接実装は User の明示指示がある例外時のみ。
- **理由**：ChatGPT の「整理・spec 化・提案」は Claude Code が、「決定・受入・release」は User が引き取れる。
- **代替案**：4者のまま → 今回のツール構成（claudecode + codex）に不要。
- **正本**：`docs/ai-roles-and-workflow.md`。

## ADR-006 名称は「PrismStar」（通称 Prism）✅（実装は Phase 2）
- **背景**：当初 `Okubo Gallery` →（検討案）`GitGallery`。だが本製品は GitHub 専用ではなく、**非エンジニアも発信する汎用プラットフォーム**。"Git" を冠すると狭く、非エンジニアを遠ざける。
- **決定**：名称を **PrismStar**（通称 Prism、タグライン `"Shine in every color."`）に確定する。
- **理由**：名前の2語がコンセプトの2軸を分担し、一語で世界観が立つ：
  - **Prism＝あらゆる色を受け入れる**（多様性・包摂）。
  - **Star＝目指せスター ＋ お気に入り(⭐) ＋ GitHub star と同じ通貨**（[ADR-009](#adr-009-評価はスターを共通通貨にする-)）。
  - 機能（多様な発信・スター評価）と名前が直結し、プロダクトとして自立した名前になる。
- **代替案**：
  - `GitGallery` → Git 色が強すぎ「git 専用」に見え、非エンジニアを排除するため却下。
  - `Folio` → `portfolio` を連想させ、プロダクト名として弱く狭いため却下。
  - `Prism` / `Beacon` 単体 → 既存プロダクトと被るため却下。
- **状態**：命名確定。表示名の実装は Phase 2。

## ADR-007 実装計画と意思決定を docs に固定する ✅
- **背景**：設計判断や実装の段取りを会話だけで進めると、後から「なぜそうしたか」を辿れず、再開・引き継ぎのたびに根拠が失われる。
- **決定**：実装計画は `docs/roadmap.md`（フェーズ）＋ `docs/phase-*-handoff.md`（Codex 向け）、技術仕様は `docs/spec.md`、意思決定は本ファイル `docs/decisions.md` に固定する。
- **理由**：「何を・なぜ・どう作ったか」と「設計と実装をどう分担したか（役割分担・handoff）」を追跡可能にし、判断の説明責任を果たす。再開・レビュー・引き継ぎが容易になる。
- **代替案**：会話だけで進める → 後から辿れず、判断の根拠が失われるため却下。

## ADR-008 「全員が発信者」プラットフォーム化 ✅
- **背景**：当初は一人の作品を飾るギャラリー。だが狙いは「**誰もが自分を発信する**」場。
- **決定**：オープンな新規登録 ＋ ユーザーごとのプロフィール / 個人ギャラリー ＋ 全員の新着フィードを備える。
- **理由**：観る人と作る人の境界をなくす。既存 `gallery.user_id` が作品を人に紐付け済みで土台がある。Phase 3 を「ただの登録」から「**発信者オンボーディング**」へ格上げする。
- **代替案**：オーナー1人だけが投稿する個人サイト → 「全員が発信者」の狙いに反するため却下。
- **関連**：`docs/roadmap.md` Phase 3、`docs/spec.md` §4–§7。

## ADR-009 評価は「スター(⭐)」を共通通貨にする ✅
- **背景**：お気に入り / 評価の仕組みをどう持つか。GitHub はリポジトリを **star** で評価する。
- **決定**：サイトのお気に入りを **スター(⭐)** とし、手動投稿作品も取り込んだ GitHub リポジトリも**同じ star 単位**で評価する。プロフィールに獲得スター総数を表示（"スターを集める"）。
- **理由**：名称（PrismStar）とコンセプトに直結。GitHub star とサイト star が地続きになり、GitHub 連携が"取ってつけ"でなく中心に溶ける。発信に「評価の循環」が加わる。
- **代替案**：like / ❤ など独自指標 → GitHub と単位が割れて統合感が薄れるため却下。
- **関連**：`docs/spec.md` §4（`stars` テーブル）/ §5（`stars.php`）、`docs/roadmap.md` Phase 5。

## ADR-010 段階的に完成させる（コンセプト全体を先に通し、機能を重ねる）✅
- **背景**：各機能を個別に作り込むより、まずコンセプト全体を一度"動く形"にして検証したい（User 方針）。
- **決定**：Phase 2–5（リブランド／発信者オンボーディング／GitHub 連携／スター）を**一気通し**でコンセプトの主軸まで実装して動く v1 を作り、その上で機能・体験を段階的に重ねる。
- **理由**：全体が動くことを早く確認でき、手戻りと過剰な作り込みを防ぐ。各 handoff は「動く最小単位」を狙い、受入基準を明確にする。
- **代替案**：各フェーズを完璧に仕上げてから次へ → 全体像の検証が遅れ、手戻りが大きくなるため却下。
- **関連**：`docs/roadmap.md`（Phase 2–5 を一気通し → 今後の発展）。

## ADR-011 token 漏れ検査は「ブラウザに配信される資産」を対象にする（github.php の文字列分割は撤去）✅
- **背景**：Phase 4 の検証で `grep -rni "GITHUB_TOKEN\|Bearer" public/` で「フロントに token が無いこと」を確認しようとした。しかし `public/` にはサーバ側で実行される PHP（`api/github.php`）も含まれ、**PHP ソースはブラウザに配信されない**（Apache が実行する）。`github.php` が `Authorization: Bearer <token>` を**サーバ側で**組み立てるのは正当だが、この grep はそれを誤検出する。Codex は検査を通すため `'GITHUB' . '_TOKEN'` / `'Bear' . 'er '` と文字列を分割した（機能・安全性は同一だが、可読性が落ち意図が不明瞭になった）。
- **決定**：
  1. **token 漏れ検査の対象は「ブラウザに配信される資産」（HTML / CSS / JS）に限定**する。サーバ側 PHP は対象外（実行されるだけでソースは露出しない）。例：`grep` を `--include=*.js --include=*.html --include=*.css` に絞る、または `public/api/*.php` を除外する。
  2. `github.php` の**文字列分割は撤去**し、`getenv('GITHUB_TOKEN')` / `'Authorization: Bearer ' . $token` の素直な表記へ戻す（次の実装パスで Codex が対応）。
- **理由**：本当の不変条件は「**token の値がブラウザに到達しない**」こと。PHP は server-side 実行なのでこれは満たされている。検査を正しく定義すれば小細工は不要で、コードは読みやすく保てる。"検査を欺く" 形を残すと、将来「なぜ分割？」という混乱と、検査自体への不信を生む。
- **代替案**：文字列分割を維持（難読化・誤解の元）→ 却下。検査を緩めるだけでコードは分割のまま（半端）→ 却下。
- **状態**：✅ 実施済み（Phase 5）。`github.php` の文字列分割を撤去し素直な表記に戻した。token 漏れ検査は配信資産（`--include=*.js --include=*.html --include=*.css`）に限定。
- **関連**：`docs/phase-4-handoff.md` の検証コマンド、`docs/spec.md` §8（safety invariants の検証）。

## ADR-012 ページを PHP 化し header/footer をサーバ側 partial で共通化 ✅（実装は Phase 6）
- **背景**：「PHP 8.2」採用なのに、ページは静的 `.html` で header/footer が全8ページに**コピペ**（DRY 違反。`PROJECT.md`「ヘッダー/フッター共通化」課題）。PHP は API 層だけで、サーバ側テンプレートを活かせていない。
- **決定**：静的 `.html` ページを `.php` に変え、header/footer（と head）を `public/includes/*.php` の **partial に1箇所化**して各ページが `include` する。**挙動は変えない構造リファクタ**。`base.html` はなごりとして `.html` のまま残す。
- **理由**：コピペ重複を解消し、ヘッダー/フッターを1箇所で管理できる。サーバ側テンプレート（`include`）という PHP らしい実装になり、ポートフォリオの説得力が増す。
- **代替案**：
  - `.html` のまま Apache に PHP 処理させる → 非標準・不自然で却下。
  - JS で header/footer を動的挿入 → クライアント依存・初期表示で崩れうる・堅牢性で劣るため却下（nav 生成は既存どおり JS でよいが、骨格はサーバ側に）。
  - 静的 `.html` のまま放置 → DRY 違反が残り「PHP プロジェクト」として弱い。却下。
- **影響**：7ページの拡張子変更 ＋ 内部リンク `.html → .php` の波及更新（`profile.js` の `repo.html_url` は**対象外**）。
- **状態**：方針確定。実装は Phase 6（`docs/phase-6-handoff.md`）。

---

> ここから先は **v2**（マイページ・公開非公開・作品作成/編集・タグ・検索）の意思決定。要件の全体像は `docs/requirements-v2.md`。

## ADR-013 マイページ（管理画面）を公開プロフィール（ユーザーページ）と別ページにする ✅（実装は Phase 8）
- **背景**：v1 の `profile.php` は「他人も見る公開ページ」と「本人の管理操作」が同居しかけていた。v2 で編集・公開/非公開・削除など管理操作が増える。
- **決定**：本人専用の**マイページ（管理画面）**を新設し、他人が見る**ユーザーページ（公開プロフィール）**と分離する。管理操作（新規作成・編集・削除・公開切替・GitHub 設定）はマイページに集約。
- **理由**：公開表示と管理の責務が分かれ、非公開作品や管理ボタンを公開ページに出さずに済む（漏洩防止）。「管理画面」という明確な機能面ができ操作の見通しが良い。
- **代替案**：1ページに統合し本人のときだけ管理 UI を出す → 公開表示と管理が混在し、条件分岐と漏洩リスクが増えるため却下。
- **関連**：`docs/requirements-v2.md`、`docs/roadmap.md` Phase 8。

## ADR-014 作品の可視性は「公開 / 非公開」の2状態 ✅（実装は Phase 7）
- **背景**：作品に下書き / 公開 / 限定公開など複数状態を持たせるか検討した。
- **決定**：**公開 / 非公開（本人のみ）の2状態**。`gallery.visibility`（既定 `public`）。非公開が「下書き」を兼ね、公開後でもいつでも非公開へ戻せる（取り下げ＝削除ではない）。
- **理由**：「下書き」と「非公開」は実質同じ（どちらも本人のみ閲覧）で別状態にする意味がない。公開/非公開だけで作成中・取り下げ・限定保管の全ユースケースを満たせ、状態が少なくクエリ・UI が単純。
- **代替案**：下書き / 公開 / **限定公開**（URL を知る人のみ）の3状態 → 限定公開（URL 共有）の要件が現状なく、実装・UI が増えるだけのため却下。要件が出たら ADR で再検討。
- **影響**：一覧・検索・ユーザーページの全 SELECT に公開フィルタ、詳細は所有者判定。**非公開を所有者以外に返さない**ことが safety invariant。
- **関連**：`docs/requirements-v2.md` §5、Phase 7。

## ADR-015 作品画像はアップロード対応（サーバ保存）✅（実装は Phase 9）
- **背景**：v1 は画像を外部 URL でのみ受け付ける。だが「**全員が発信者（非エンジニアも）**」が核で、URL 入力を要求する UX は致命的。
- **決定**：画像ファイルの**アップロード**に対応し、`public/uploads/` に保存して `gallery.src` にパスを持つ（外部 URL も併存可）。
- **理由**：非エンジニアが普通に発信できる導線はコンセプトの核であり、単なる polish ではない。
- **代替案**：URL のみ維持 → 非エンジニアを実質排除し「全員が発信者」に反するため却下（ただし段階的に Phase 9 で）。
- **影響（safety invariant 追加）**：MIME/拡張子/サイズ検証、ファイル名はサーバ生成、保存先固定、実行可能ファイル拒否（パストラバーサル防止）。
- **関連**：`docs/requirements-v2.md` §8、Phase 9。

## ADR-016 GitHub リポジトリは作品テーブルに永続化しない（v2 スコープ）✅
- **背景**：v2 で検索・可視性・マイページ管理を入れるにあたり、GitHub 取り込み repo をサイト作品（`gallery`）として永続化するか。
- **決定**：**永続化しない**。GitHub repo は従来どおりプロフィール/マイページの**表示レイヤー**（サーバ側 proxy で都度取得）に留め、検索・可視性・編集の対象は**サイト内作品とユーザー**に限定する。
- **理由**：永続化は同期・重複処理・source 種別・OG 画像取得など実装が一気に重くなる。v2 の主軸（管理・可視性・検索）を先に通すほうが価値が高い（ADR-010 段階的）。スター統合（ADR-009）は表示レイヤーのままでも維持できる。
- **代替案**：repo を `gallery` に取り込み第一級作品にする → スコープ肥大のため今回は却下。今後の発展として残す。
- **関連**：`docs/requirements-v2.md` §9、`docs/roadmap.md` 今後の発展。

## ADR-017 タグは正規化テーブル（多対多）で持つ ✅（実装は Phase 10）
- **背景**：検索を「ワード＋タグ」で行うため作品にタグを付与する。保持方法（カンマ区切り文字列 vs 正規化テーブル）を検討。
- **決定**：`tags`（マスタ）＋ `gallery_tags`（中間）の**多対多**で正規化して持つ。
- **理由**：表記揺れ防止・件数集計・タグページ等への発展がしやすく、リレーショナル設計として素直。DB 設計を明示できる。
- **代替案**：`gallery.tags` にカンマ区切り文字列 → 実装は最小だが集計・正規化・拡張に弱く検索も曖昧になるため却下。
- **関連**：`docs/requirements-v2.md` §4、Phase 10。
